.\.venv\Scripts\Activate.ps1  // ativa a venv

deactivate // desativa a venv

py python_service/app.py //ativa o python_service

//usar git
git add .
git commit -m "mudanças"
git push origin main
//


//quando for usar em outro computador
py -m venv .venv //cria a venv

pip install -r python_service/requirements.txt //instala as bibliotecas

FLASK_HOST=127.0.0.1
FLASK_PORT=5000  //arquivo .env dentro da pasta python_service

